/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { UpgradetoemployeeService } from './Upgradetoemployee.service';

describe('Service: Upgradetoemployee', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [UpgradetoemployeeService]
    });
  });

  it('should ...', inject([UpgradetoemployeeService], (service: UpgradetoemployeeService) => {
    expect(service).toBeTruthy();
  }));
});
